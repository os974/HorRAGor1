# --- TMDB API ---
TMDB_TOKEN=

# --- Base de données ---
# Absent => SQLite local (data/horragor.db).
# Pour déployer sur Supabase, décommenter et renseigner la connection string
# (Project Settings > Database > Connection string > URI), en remplaçant le
# schéma par psycopg :
# DATABASE_URL=postgresql+psycopg://postgres:<password>@<host>:5432/postgres

# --- Supabase (optionnel : API REST/Storage) ---
SUPABASE_URL=
SUPABASE_KEY=
